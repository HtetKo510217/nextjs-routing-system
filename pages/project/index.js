// if project.js create before it will run , Otherwise create project folder first it will run

function Project () {
    return (
        <div>Folder base project</div>
    )
}

export default Project;